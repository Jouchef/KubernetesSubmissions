apiVersion: apps/v1
kind: Deployment
metadata:
  name: server-dep
spec:
  replicas: 1
  selector:
    matchLabels:
      app: server
  template:
    metadata:
      labels:
        app: server
    spec:
      volumes:
        - name: server-volume
          persistentVolumeClaim:
            claimName: server-claim
      containers:
        - name: server-container
          image: jouchef/server:1.12
          env:
            - name: PORT
              value: "3006"
          volumeMounts:
            - name: server-volume
              mountPath: /app/static